// constants for what a seed does
var SEED_FALLING = 0;
var SEED_RESTING = 1;
var SEED_DIGGING = 2;
var SEED_GROWING = 3;

function setup() {
  createCanvas(windowWidth, windowHeight);
  background(0);
}

function draw() {
  
}

/**
 * Gives a seed the following functionallity:
 * falling, resting, digging, growing
 */ 
function plantSeed(x, y) {
  
}